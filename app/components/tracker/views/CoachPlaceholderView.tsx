export function CoachPlaceholderView() {
  return (
    <section className="panel mt-4">
      <p className="eyebrow">AI Coach</p>
      <h2 className="section-title">Coach review is warming up</h2>
      <p className="mt-3 max-w-2xl text-sm leading-6 text-stone-600">
        Once workout history, preferences, skipped body parts, progression, and
        weak areas are reliable, this surface can turn the logs into a
        structured progression plan.
      </p>
      <div className="mt-5 rounded-[16px] border border-[var(--line)] bg-[var(--surface-rail)] p-4 text-sm font-semibold text-[var(--muted)]">
        Keep logging working sets and completed sessions to unlock more useful
        coaching signals.
      </div>
    </section>
  );
}
